<?xml version="1.0" encoding="utf-8"?>
<manifest
        package="com.karaokeapp"
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:versionCode="1"
        android:versionName="1.0">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <uses-sdk
            android:minSdkVersion="8"
            android:targetSdkVersion="19"/>

    <application
            android:allowBackup="true"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name"
            android:name=".KaraokeApp"
            android:theme="@style/AppTheme">
        <activity
                android:name="com.karaokeapp.ActivityLogin"
                android:label="@string/app_name">"
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
                android:name="com.karaokeapp.ActivityMain"
                android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH"/>
            </intent-filter>

            <meta-data
                    android:name="android.app.searchable"
                    android:resource="@xml/searchable"/>
        </activity>

        <provider
                android:authorities="com.karaokeapp.android"
                android:name=".data.KaraokeProvider"
                android:exported="false"
                android:syncable="true"/>

        <service android:name=".DataStoreService"
                android:exported="false"/>

    </application>

</manifest>
